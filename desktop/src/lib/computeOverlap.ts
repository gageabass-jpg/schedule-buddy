// The coverage engine — protected rest, and the windows nobody is home for.
// Defined in shared/ so nucleusAI can answer with the same numbers the
// Childcare tab draws, rather than an approximation of them.
export * from "../../../shared/computeOverlap";
