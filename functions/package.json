{
  "name": "schedule-buddy-functions",
  "version": "0.1.0",
  "description": "Cloud Functions for Schedule Buddy — pushes when coverage requests change.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "private": true
}
