# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase
.firebase/
*.log

# Capacitor: web bundle is copied into ios/App/App/public on `npx cap sync`.
# It's a build artifact — the source of truth lives in /public.
ios/App/App/public/

# Xcode user state (per-developer, not for the repo)
**/xcuserdata/
*.xcuserstate
*.xcuserdatad/
*.xcscmblueprint
*.xccheckout

# Xcode derived data / build output
DerivedData/
build/
# …but DO ship desktop/build/icon.png — electron-builder uses it.
!desktop/build/
!desktop/build/icon.png
*.ipa
*.dSYM
*.dSYM.zip

# CocoaPods (we're using SPM, but in case)
Pods/
*.xcworkspace/contents.xcworkspacedata.bak

# Capacitor SPM checkouts (re-fetched on build)
ios/App/CapApp-SPM/Package.resolved.bak

# Editor cruft
.vscode/
.idea/
*.swp
*~

# Env / secrets — none committed; placeholder
.env
.env.local

# Post Buddy — tracking API creds + raw tracking input (full tracking #s stay
# local; only public/post-buddy-data.json with MASKED numbers is committed/deployed)
.easypost.key
.usps.key
scripts/packages.input.json
"desktop/dist-electron 2/"
desktop/release/

# macOS Finder duplicates
* 2/
* 3/
* 2.xml
* 3.xml
k-schedule/

# Claude Code: machine-local settings and transient agent worktrees
# (worktrees hold full nested checkouts — never commit them)
.claude/settings.local.json
.claude/worktrees/
